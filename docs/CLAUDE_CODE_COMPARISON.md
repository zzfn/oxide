# Oxide vs Claude Code 功能对比与改进计划

> 最后更新: 2026-01-28
> 状态: 规划中

本文档记录 Oxide 项目与 Anthropic Claude Code 的功能差距，用于指导后续开发工作。

---

## 📊 功能对比总览

| 功能模块 | Claude Code | Oxide | 状态 | 优先级 |
|---------|-------------|-------|------|--------|
| **计划模式** | ✅ EnterPlanMode/ExitPlanMode | ❌ 缺失 | 待实现 | 🔴 高 |
| **任务管理** | ✅ 完整（依赖、元数据） | ⚠️ 基础框架 | 待完善 | 🔴 高 |
| **Web 工具** | ✅ WebFetch/WebSearch | ❌ 缺失 | 待实现 | 🟡 中 |
| **多模态** | ✅ 图片/PDF/Notebook | ⚠️ 部分 | 待完善 | 🟡 中 |
| **Git 工作流** | ✅ 完整协议 + PR | ⚠️ 基础工具 | 待完善 | 🔴 高 |
| **子代理系统** | ✅ Task 工具 + 后台运行 | ⚠️ 无自主启动 | 待实现 | 🔴 高 |
| **用户交互** | ✅ AskUserQuestion | ⚠️ 未集成 | 待集成 | 🟡 中 |
| **高级编辑** | ✅ MultiEdit/NotebookEdit | ⚠️ 未集成 | 待集成 | 🟢 低 |
| **Shell 增强** | ✅ 后台执行 + TaskOutput | ⚠️ 基础执行 | 待完善 | 🟡 中 |
| **MCP 集成** | ✅ 完整支持 | ❌ 未实现 | 待实现 | 🟡 中 |
| **远程会话** | ✅ 云端协作 | ❌ 缺失 | 待实现 | 🟢 低 |
| **内置技能** | ✅ 10+ 专业技能 | ⚠️ 3 个基础技能 | 待扩展 | 🟡 中 |
| **代码引用** | ✅ file:line 格式 | ❌ 未提及 | 待实现 | 🟢 低 |
| **上下文管理** | ✅ 自动摘要 + 预算 | ⚠️ 基础管理 | 待完善 | 🟡 中 |
| **Hooks 系统** | ✅ 事件响应 | ❌ 缺失 | 待实现 | 🟢 低 |

---

## 🔴 高优先级功能（核心差距）

### 1. 计划模式系统

**状态**: ❌ 缺失
**优先级**: 🔴 高
**预计工作量**: 中等

#### Claude Code 功能
- `EnterPlanMode` 工具 - 进入计划模式
- `ExitPlanMode` 工具 - 退出计划模式并请求用户批准
- 独立的计划文件写入和审批流程
- 允许的权限提示（allowedPrompts）
- 计划可推送到远程会话

#### Oxide 现状
- ✅ 有 Plan Agent 类型
- ❌ 没有独立的计划模式工作流
- ⚠️ PAOR 工作流只是结构化提示，不是真正的计划-审批循环

#### 实现建议
1. **新增工具**:
   - `src/tools/enter_plan_mode.rs` - 进入计划模式
   - `src/tools/exit_plan_mode.rs` - 退出计划模式
2. **计划文件管理**:
   - 计划文件路径: `.oxide/plans/<plan_id>.md`
   - 支持计划版本控制
3. **用户审批流程**:
   - 显示计划内容
   - 用户确认/拒绝/修改
   - 批准后执行
4. **权限管理**:
   - 定义 `AllowedPrompt` 结构
   - 在计划中声明需要的权限
   - 执行前验证权限

#### 相关文件
- `src/agent/workflow/` - 可以复用部分 PAOR 逻辑
- `src/tools/` - 添加新工具
- `src/cli/command.rs` - 添加 `/plan` 命令

---

### 2. 任务管理系统完善

**状态**: ⚠️ 基础框架存在
**优先级**: 🔴 高
**预计工作量**: 中等

#### Claude Code 功能
- `TaskCreate` - 创建任务（带 activeForm、description、metadata）
- `TaskUpdate` - 更新任务状态（pending/in_progress/completed/deleted）
- `TaskList` - 列出所有任务
- `TaskGet` - 获取任务详情
- 任务依赖关系（blocks/blockedBy）
- 任务所有者（owner）
- 任务元数据（metadata）

#### Oxide 现状
- ⚠️ 基础框架存在（`src/task/`）
- ❌ 未集成到主 Agent
- ❌ 没有依赖关系管理
- ❌ 没有任务元数据
- ❌ 没有 activeForm（进行中显示文本）

#### 实现建议
1. **完善数据结构**:
```rust
pub struct Task {
    pub id: String,
    pub subject: String,           // 任务标题
    pub description: String,       // 详细描述
    pub active_form: String,       // 进行中显示文本（如 "Running tests"）
    pub status: TaskStatus,        // pending/in_progress/completed/deleted
    pub owner: Option<String>,     // 任务所有者
    pub blocks: Vec<String>,       // 阻塞的任务 ID
    pub blocked_by: Vec<String>,   // 被阻塞的任务 ID
    pub metadata: HashMap<String, serde_json::Value>,  // 元数据
    pub created_at: DateTime<Utc>,
    pub updated_at: DateTime<Utc>,
}
```

2. **实现工具**:
   - `TaskCreate` - 创建任务
   - `TaskUpdate` - 更新任务（支持增量更新）
   - `TaskList` - 列出任务（支持过滤）
   - `TaskGet` - 获取任务详情

3. **依赖关系管理**:
   - 检测循环依赖
   - 自动解锁被阻塞的任务
   - 任务完成时通知依赖任务

4. **集成到主 Agent**:
   - 添加到 `AgentBuilder` 的工具列表
   - 在系统提示词中说明任务管理最佳实践

#### 相关文件
- `src/task/` - 完善现有代码
- `src/tools/task.rs` - 已存在但需完善
- `src/agent/builder.rs` - 集成工具

---

### 3. Task 工具（子代理自主启动）

**状态**: ❌ 缺失
**优先级**: 🔴 高
**预计工作量**: 大

#### Claude Code 功能
- `Task` 工具让 Agent 可以自主启动子代理
- 支持多种专业子代理类型：
  - `Bash` - 命令执行专家
  - `general-purpose` - 通用代理
  - `Explore` - 代码库探索（快速/中等/深入）
  - `Plan` - 软件架构师
  - `code-reviewer` - 代码审查专家
  - `quant-analyst` - 量化分析专家
  - `frontend-developer` - 前端开发专家
  - `feature-dev:*` - 功能开发系列
- 子代理可以后台运行（`run_in_background`）
- 子代理可以恢复（`resume` 参数）
- 子代理有独立的工具权限
- 支持指定模型（haiku/sonnet/opus）

#### Oxide 现状
- ✅ 有 6 种 Agent 类型（Main, Explore, Plan, CodeReviewer, FrontendDeveloper, General）
- ✅ 有 SubagentManager
- ❌ 没有 `Task` 工具让 Agent 自己启动子代理
- ❌ 没有后台运行支持
- ❌ 没有恢复机制

#### 实现建议
1. **新增 Task 工具**:
```rust
pub struct TaskTool {
    pub description: String,           // 任务描述（3-5 词）
    pub prompt: String,                // 详细任务提示
    pub subagent_type: String,         // 子代理类型
    pub model: Option<String>,         // 可选模型（haiku/sonnet/opus）
    pub run_in_background: bool,       // 是否后台运行
    pub resume: Option<String>,        // 恢复的 agent ID
    pub max_turns: Option<usize>,      // 最大轮次
}
```

2. **后台运行支持**:
   - 使用 Tokio 异步任务
   - 输出重定向到文件（`.oxide/tasks/<task_id>.log`）
   - 提供 `TaskOutput` 工具读取输出
   - 提供 `TaskStop` 工具停止任务

3. **恢复机制**:
   - 保存子代理的完整上下文
   - 支持从上次中断处继续
   - 恢复时保留历史消息

4. **新增专业子代理**:
   - `Bash` - 专注于命令执行
   - `QuantAnalyst` - 量化分析
   - `FeatureDev` 系列 - 功能开发工作流

#### 相关文件
- `src/tools/task.rs` - 已存在但需重写
- `src/tools/task_output.rs` - 已存在但需完善
- `src/agent/subagent_manager.rs` - 扩展功能
- `src/agent/agent_type.rs` - 添加新 Agent 类型

---

### 4. Git 工作流完善

**状态**: ⚠️ 基础工具存在
**优先级**: 🔴 高
**预计工作量**: 中等

#### Claude Code 功能
- 详细的 Git 安全协议：
  - 禁止 force push 到 main/master
  - 禁止跳过 hooks（--no-verify）
  - 禁止修改 git config
  - Pre-commit hook 失败后创建新 commit（不用 --amend）
- Commit message 自动生成：
  - 分析 `git status` 和 `git diff`
  - 参考 `git log` 学习项目风格
  - 遵循 Conventional Commits
  - 添加 Co-Authored-By 标签
- PR 创建工作流：
  - 使用 `gh` CLI
  - 自动生成 PR 标题和描述
  - 检查 CI 状态

#### Oxide 现状
- ✅ 有 `git_guard` 工具
- ✅ 有 `commit_linter` 工具
- ❌ 没有完整的 Git 工作流集成
- ❌ 没有 PR 创建功能
- ❌ 没有自动生成 commit message

#### 实现建议
1. **完善 git_guard**:
   - 添加更多安全检查
   - 检测敏感文件（.env, credentials.json）
   - 警告大文件提交

2. **实现 Commit 工作流**:
   - 新增 `/commit` 技能（已有但需完善）
   - 自动运行 `git status`, `git diff`, `git log`
   - 生成符合项目风格的 commit message
   - 添加 Co-Authored-By 标签

3. **实现 PR 工作流**:
   - 新增 `/pr` 技能
   - 集成 `gh` CLI
   - 自动生成 PR 描述
   - 检查 CI 状态

4. **在系统提示词中添加 Git 协议**:
   - 详细的安全规则
   - Commit 最佳实践
   - PR 创建流程

#### 相关文件
- `src/tools/git_guard.rs` - 完善现有代码
- `src/tools/commit_linter.rs` - 完善现有代码
- `skills/commit.md` - 完善技能
- `skills/pr.md` - 新增技能
- `src/agent/preamble/` - 添加 Git 协议到系统提示词

---

## 🟡 中优先级功能（增强体验）

### 5. Web 工具

**状态**: ❌ 缺失
**优先级**: 🟡 中
**预计工作量**: 中等

#### Claude Code 功能
- `WebFetch` - 获取网页内容并用 AI 处理
  - 自动 HTTP → HTTPS 升级
  - HTML 转 Markdown
  - 15 分钟缓存机制
  - 重定向处理
- `WebSearch` - 网页搜索（通过 MCP）

#### 实现建议
1. **新增 WebFetch 工具**:
```rust
pub struct WebFetchTool {
    pub url: String,
    pub prompt: String,  // 要提取的信息
}
```

2. **依赖库**:
   - `reqwest` - HTTP 客户端
   - `html2text` 或 `scraper` - HTML 解析
   - `lru` - LRU 缓存

3. **功能实现**:
   - URL 验证和规范化
   - 自动 HTTPS 升级
   - 重定向处理（最多 5 次）
   - HTML 转 Markdown
   - 缓存机制（15 分钟 TTL）
   - 用小模型处理内容（节省成本）

#### 相关文件
- `src/tools/web_fetch.rs` - 新增
- `Cargo.toml` - 添加依赖

---

### 6. 多模态支持

**状态**: ⚠️ 部分实现
**优先级**: 🟡 中
**预计工作量**: 中等

#### Claude Code 功能
- 读取图片（PNG, JPG, GIF, WebP 等）
- 读取 PDF 文件（逐页提取文本和视觉内容）
- 读取 Jupyter Notebook（.ipynb）
- 截图分析

#### Oxide 现状
- ✅ 有 `notebook_edit` 工具（但未集成）
- ❌ 没有图片读取
- ❌ 没有 PDF 读取
- ⚠️ `enable_multimodal` 配置存在但未实现

#### 实现建议
1. **扩展 read_file 工具**:
   - 检测文件类型（MIME type）
   - 图片文件 → 转为 base64 + 视觉内容
   - PDF 文件 → 逐页提取文本和图像
   - Notebook 文件 → 解析 JSON 结构

2. **依赖库**:
   - `image` - 图片处理
   - `pdf` 或 `lopdf` - PDF 解析
   - `base64` - 编码

3. **API 集成**:
   - 使用 Claude 的视觉 API
   - 支持多模态消息格式

#### 相关文件
- `src/tools/read_file.rs` - 扩展现有代码
- `src/tools/notebook_edit.rs` - 集成到主 Agent
- `Cargo.toml` - 添加依赖

---

### 7. 集成已有工具

**状态**: ⚠️ 已实现但未集成
**优先级**: 🟡 中
**预计工作量**: 小

#### 待集成工具
1. **multiedit** - 多文件批量编辑
2. **notebook_edit** - Jupyter Notebook 编辑
3. **ask_user_question** - 用户交互
4. **search_replace** - 块级代码替换

#### 实现建议
1. **添加到 AgentBuilder**:
```rust
// src/agent/builder.rs
pub fn build_main_agent() -> Agent {
    let tools = vec![
        // ... 现有工具
        Box::new(MultiEditTool::new()),
        Box::new(NotebookEditTool::new()),
        Box::new(AskUserQuestionTool::new()),
        Box::new(SearchReplaceTool::new()),
    ];
    // ...
}
```

2. **更新系统提示词**:
   - 说明新工具的用途
   - 提供使用示例

#### 相关文件
- `src/agent/builder.rs` - 添加工具
- `src/agent/preamble/` - 更新提示词

---

### 8. Shell 后台执行

**状态**: ⚠️ 基础执行存在
**优先级**: 🟡 中
**预计工作量**: 中等

#### Claude Code 功能
- 后台执行（`run_in_background`）
- 任务输出管理（`TaskOutput`）
- 任务停止（`TaskStop`）
- 详细的使用指南（避免使用 find/grep/cat 等）

#### Oxide 现状
- ✅ 有 `shell_execute` 工具
- ❌ 没有后台执行
- ⚠️ `task_output` 工具存在但未集成

#### 实现建议
1. **扩展 shell_execute**:
```rust
pub struct ShellExecuteTool {
    pub command: String,
    pub description: String,
    pub timeout: Option<u64>,
    pub run_in_background: bool,  // 新增
}
```

2. **后台任务管理**:
   - 使用 Tokio spawn
   - 输出重定向到文件
   - 返回 task_id
   - 提供 `TaskOutput` 工具读取输出
   - 提供 `TaskStop` 工具停止任务

3. **在系统提示词中添加指南**:
   - 何时使用后台执行
   - 如何检查任务状态
   - 避免使用 find/grep/cat 等（用专用工具）

#### 相关文件
- `src/tools/shell_execute.rs` - 扩展现有代码
- `src/tools/task_output.rs` - 完善现有代码
- `src/agent/preamble/` - 添加使用指南

---

### 9. 内置技能扩展

**状态**: ⚠️ 基础技能存在
**优先级**: 🟡 中
**预计工作量**: 大（持续）

#### Claude Code 技能
- `ui-ux-pro-max` - UI/UX 设计（50 种样式、21 种调色板）
- `web-design-guidelines` - Web 界面审查
- `skill-creator` - 技能创建指南
- `codebase-visualizer` - 代码库可视化
- `vercel-react-best-practices` - React 最佳实践
- `git-commit` - Git 工作流专家
- `code-review:code-review` - PR 审查
- `commit-commands:*` - Commit/Push/PR 系列
- `feature-dev:feature-dev` - 功能开发指南
- `frontend-design` - 前端设计

#### Oxide 现状
- ✅ 技能系统完整（分层加载、参数化）
- ⚠️ 内置技能较少（只有 commit, compact, review）

#### 实现建议
1. **优先添加的技能**:
   - `git-workflow` - 完整的 Git 工作流
   - `code-review` - 代码审查清单
   - `feature-dev` - 功能开发指南
   - `skill-creator` - 技能创建指南

2. **技能文件位置**:
   - `skills/git-workflow.md`
   - `skills/code-review.md`
   - `skills/feature-dev.md`
   - `skills/skill-creator.md`

3. **技能内容**:
   - 详细的步骤说明
   - 最佳实践
   - 常见陷阱
   - 示例

#### 相关文件
- `skills/` - 添加新技能文件

---

### 10. 上下文自动摘要

**状态**: ❌ 缺失
**优先级**: 🟡 中
**预计工作量**: 大

#### Claude Code 功能
- 无限上下文（通过自动摘要）
- 预算管理（token_budget）
- 系统提醒（system-reminder）

#### Oxide 现状
- ✅ 有会话管理
- ✅ 有 Token 计数
- ❌ 没有自动摘要
- ❌ 没有预算管理

#### 实现建议
1. **摘要策略**:
   - 当消息数超过阈值时触发
   - 使用小模型（haiku）生成摘要
   - 保留最近 N 条消息
   - 将旧消息摘要为单条消息

2. **预算管理**:
```rust
pub struct TokenBudget {
    pub max_tokens: usize,
    pub used_tokens: usize,
    pub warning_threshold: f32,  // 0.8 = 80%
}
```

3. **实现流程**:
   - 每次对话后计算 Token 使用
   - 接近预算时警告用户
   - 超过预算时自动摘要

#### 相关文件
- `src/context.rs` - 添加摘要功能
- `src/token_counter.rs` - 扩展功能
- `src/config/mod.rs` - 添加预算配置

---

## 🟢 低优先级功能（锦上添花）

### 11. MCP 集成

**状态**: ❌ 未实现
**优先级**: 🟢 低
**预计工作量**: 大

#### 实现建议
- 参考 MCP 协议规范
- 实现 MCP 客户端
- 支持动态工具注册
- 配置开关已存在（`enable_mcp`）

#### 相关文件
- `src/mcp/` - 新增模块
- `src/config/mod.rs` - 使用现有配置

---

### 12. Hooks 系统

**状态**: ❌ 缺失
**优先级**: 🟢 低
**预计工作量**: 中等

#### Claude Code 功能
- 用户可配置 hooks（shell 命令响应事件）
- `<user-prompt-submit-hook>` 等
- Hook 反馈被视为用户输入

#### 实现建议
1. **配置格式**:
```toml
[hooks]
on_prompt_submit = "echo 'User submitted: $PROMPT'"
on_tool_call = "echo 'Tool called: $TOOL_NAME'"
on_error = "notify-send 'Error occurred'"
```

2. **事件类型**:
   - `on_prompt_submit` - 用户提交提示
   - `on_tool_call` - 工具调用
   - `on_error` - 错误发生
   - `on_session_start` - 会话开始
   - `on_session_end` - 会话结束

#### 相关文件
- `src/hooks/` - 新增模块
- `src/config/mod.rs` - 添加 hooks 配置

---

### 13. 远程会话

**状态**: ❌ 缺失
**优先级**: 🟢 低
**预计工作量**: 大

#### Claude Code 功能
- 推送计划到远程 Claude.ai 会话
- `remoteSessionId`, `remoteSessionUrl`, `remoteSessionTitle`

#### 实现建议
- 需要 Claude.ai API 支持
- 实现会话同步机制
- 低优先级，除非需要云端协作

---

### 14. 代码引用格式

**状态**: ❌ 未提及
**优先级**: 🟢 低
**预计工作量**: 小

#### Claude Code 功能
- 自动生成 `file_path:line_number` 格式的引用
- 方便用户导航到源代码位置

#### 实现建议
- 在系统提示词中添加代码引用格式要求
- 示例: "错误发生在 src/main.rs:42"

#### 相关文件
- `src/agent/preamble/` - 更新提示词

---

## 📋 实施路线图

### Phase 1: 快速改进（1-2 周）
- [ ] 集成已有工具（multiedit, notebook_edit, ask_user_question）
- [ ] 完善 Git 工作流（commit 自动生成、PR 创建）
- [ ] 添加代码引用格式到系统提示词
- [ ] 添加 2-3 个常用技能（git-workflow, code-review）

### Phase 2: 核心功能（3-4 周）
- [ ] 实现计划模式系统（EnterPlanMode/ExitPlanMode）
- [ ] 完善任务管理系统（依赖关系、元数据）
- [ ] 实现 Task 工具（子代理自主启动）
- [ ] Shell 后台执行支持

### Phase 3: 增强功能（4-6 周）
- [ ] 实现 WebFetch 工具
- [ ] 多模态支持（图片、PDF）
- [ ] 上下文自动摘要
- [ ] 添加更多专业子代理（QuantAnalyst, FeatureDev 系列）

### Phase 4: 高级功能（长期）
- [ ] MCP 集成
- [ ] Hooks 系统
- [ ] 远程会话支持
- [ ] 持续添加内置技能

---

## 🎯 成功指标

### 功能完整性
- [ ] 核心工具覆盖率 ≥ 90%
- [ ] 子代理类型 ≥ 8 种
- [ ] 内置技能 ≥ 10 个

### 用户体验
- [ ] 计划模式可用
- [ ] 任务管理完整
- [ ] 后台任务支持
- [ ] 多模态支持

### 代码质量
- [ ] 所有新功能有单元测试
- [ ] 文档完整更新
- [ ] 无已知严重 Bug

---

## 📝 更新日志

### 2026-01-28
- 初始版本
- 完成功能对比分析
- 制定实施路线图

---

## 🔗 相关文档

- [架构文档](./architecture.md)
- [Agent 系统详解](./agent-system.md)
- [工具系统详解](./tool-system.md)
- [PAOR 工作流指南](./PAOR_WORKFLOW.md)
- [技能系统详解](./skill-system.md)
