# Oxide 架构文档

## 📚 系统详解

本文档提供了 Oxide 的整体架构概览。对于各个核心系统的深入实现细节，请参考以下专门文档：

- **[Agent 系统](./agent-system.md)** - 多 Agent 架构、LLM 集成、工具管理
- **[技能系统](./skill-system.md)** - 技能加载、模板渲染、命令抽象
- **[工具系统](./tool-system.md)** - 文件操作、搜索、Shell 执行工具
- **[配置管理](./config-management.md)** - 分层配置、环境变量、验证机制
- **[会话管理](./session-management.md)** - 消息持久化、多会话管理、自动清理

## 项目概述

Oxide 是一个基于 Rust 开发的命令行 AI 助手工具，支持多种 LLM 提供商（如 DeepSeek、OpenAI、Anthropic），提供智能对话和工具调用功能。项目采用混合架构，主要代码使用 Rust，同时提供 Node.js 包装器用于 npm 发布。

## 整体架构

### 分层设计

```
┌─────────────────────────────────────┐
│         CLI 层 (用户交互)            │
│  - 命令解析                          │
│  - 交互式选择器                      │
│  - 结果渲染                          │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│    Agent 层 (AI 代理 + 技能系统)     │
│  - 多种 Agent 类型                   │
│  - 技能管理器                        │
│  - 上下文管理                        │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│       Tools 层 (工具实现)            │
│  - 文件操作                          │
│  - Shell 执行                        │
│  - 代码搜索                          │
│  - Git 集成                          │
└─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────┐
│    Context 层 (会话管理 + 配置)      │
│  - 会话持久化                        │
│  - 配置管理                          │
│  - 历史记录                          │
└─────────────────────────────────────┘
```

## 核心模块

### 1. Agent 系统

#### Agent 类型

项目支持多种专用 Agent，每种 Agent 针对特定场景优化：

- **Main Agent**: 主对话代理，具有完整的工具访问权限
- **Explore Agent**: 只读代理，用于代码库探索和分析
- **Plan Agent**: 架构规划代理，用于设计实现方案
- **Code Reviewer Agent**: 代码审查代理，专注于代码质量分析
- **Frontend Developer Agent**: 前端开发专用代理

#### Agent 构建器模式

使用 `AgentBuilder` 实现灵活的 Agent 创建：

```rust
// 伪代码示例
let agent = AgentBuilder::new()
    .provider(Anthropic)
    .model("claude-3-5-sonnet")
    .system_prompt(custom_prompt)
    .with_tools(tools)
    .build();
```

**特性**：
- 支持多种 LLM 提供商（Anthropic、OpenAI、DeepSeek）
- 可配置的系统提示词
- 灵活的工具权限控制
- 工厂模式创建特定类型 Agent

### 2. 技能系统 (Skill System)

#### 技能来源优先级

1. **本地技能**: `.oxide/skills/` - 项目特定技能
2. **全局技能**: `~/.oxide/skills/` - 跨项目共享技能
3. **内置技能**: 硬编码在应用中的核心技能

#### 技能格式

技能使用 Markdown + Front Matter 格式：

```markdown
---
description: "创建 Git commit"
triggers: ["commit", "ci"]
args:
  - name: message
    description: "Commit 消息"
    required: true
---

# 创建 Git Commit

请执行以下操作：
1. 检查 git 状态
2. 添加所有更改
3. 创建 commit：{{message}}
```

#### 技能管理

- **加载器**: 按优先级从不同来源加载技能
- **执行器**: 渲染模板并替换参数
- **缓存**: 使用 `Lazy + RwLock` 避免重复加载

### 3. 工具系统

#### 工具接口定义

所有工具实现统一的 `Tool` trait：

```rust
pub trait Tool {
    const NAME: &'static str;
    type Error;
    type Args;
    type Output;

    async fn definition(&self, prompt: String) -> ToolDefinition;
    async fn call(&self, args: Self::Args) -> Result<Self::Output, Self::Error>;
}
```

#### 主要工具分类

**文件操作工具**：
- `read_file` - 文件读取
- `write_file` - 文件写入
- `edit_file` - 基于 diff 的文件编辑
- `delete_file` - 文件删除

**代码搜索工具**：
- `grep` - 正则表达式搜索
- `glob` - 文件模式匹配
- `scan_codebase` - 代码库扫描

**系统工具**：
- `shell_execute` - Shell 命令执行
- `git_operations` - Git 操作集成

#### 工具规范

- 遵循 OpenAI Function Calling 规范
- 异步执行模型
- 统一的错误处理
- 安全的权限控制

### 4. 配置管理

#### 配置加载优先级

1. **环境变量** (最高优先级)
2. **项目配置** (`.oxide/config.toml`)
3. **项目指令** (`.oxide/CONFIG.md`)
4. **默认配置** (硬编码)

#### 配置项

- LLM 提供商和 API 密钥
- 模型选择
- Agent 类型
- 技能路径
- 会话存储路径

### 5. 会话管理

#### 会话生命周期

```rust
Session {
    id: String,           // 唯一标识符
    messages: Vec<Message>, // 消息历史
    agent_type: String,    // Agent 类型
    created_at: DateTime,  // 创建时间
}
```

#### 功能特性

- 自动生成唯一会话 ID
- 多轮对话历史持久化
- 支持会话加载、删除、管理
- 上下文保持和传递

## CLI 交互

### 触发器系统

支持三种触发符：

- `/` - 命令菜单（技能、Agent 切换等）
- `@` - 上下文引用（文件、符号等）
- `#` - 标签/话题

### 交互式特性

- **模糊搜索**: 使用 `fuzzy-matcher` 实现快速命令搜索
- **自动补全**: 文件路径、命令历史提示
- **多选模式**: 支持批量操作
- **彩色输出**: 使用 `colored` crate 提供视觉反馈

### 渲染系统

```rust
pub enum RenderFormat {
    Markdown,
    Json,
    Plain,
}

pub fn render(output: Output, format: RenderFormat) -> String {
    // 格式化输出
}
```

## 技术栈

### 核心技术

| 技术 | 用途 | 版本要求 |
|------|------|----------|
| Rust | 主要开发语言 | 2021 edition |
| Tokio | 异步运行时 | latest |
| Rig | LLM 应用框架 | latest |
| Serde | 序列化/反序列化 | latest |

### CLI 和 UI

| 库 | 用途 |
|---|---|
| Rustyline | 高级命令行编辑器 |
| Dialoguer | 交互式选择器 |
| Colored | 终端彩色输出 |

### LLM 集成

- **Anthropic Claude**: claude-3-5-sonnet, claude-4-opus
- **OpenAI**: gpt-4o, gpt-4o-mini
- **DeepSeek**: deepseek-chat, deepseek-coder
- **其他**: OpenAI 兼容 API

### 文件系统

- **patch-apply**: unified diff 应用
- **Walkdir + Ignore**: 高效文件系统遍历
- **Git2**: Git 集成
- **Regex**: 正则表达式搜索

## 设计模式

### 1. 构建器模式 (Builder Pattern)

用于复杂对象创建，特别是 Agent 配置：

```rust
AgentBuilder::new()
    .provider(Provider::Anthropic)
    .model("claude-3-5-sonnet")
    .with_tools(tools)
    .build()
```

### 2. 工厂模式 (Factory Pattern)

创建不同类型的 Agent：

```rust
pub enum AgentType {
    Main,
    Explore,
    Plan,
    CodeReviewer,
    FrontendDeveloper,
}

impl AgentType {
    pub fn create_agent(&self) -> Agent {
        match self {
            AgentType::Explore => AgentBuilder::new()
                .read_only()
                .build(),
            // ...
        }
    }
}
```

### 3. 观察者模式 (Observer Pattern)

ContextManager 管理会话状态变化：

```rust
pub struct ContextManager {
    sessions: Arc<RwLock<HashMap<String, Session>>>,
    listeners: Vec<Box<dyn ContextListener>>,
}
```

### 4. 策略模式 (Strategy Pattern)

不同的 LLM 提供商实现统一的接口：

```rust
pub trait LLMProvider {
    async fn chat(&self, messages: Vec<Message>) -> Response;
}

impl LLMProvider for AnthropicProvider { /* ... */ }
impl LLMProvider for OpenAIProvider { /* ... */ }
```

## 目录结构

```
oxide/
├── src/
│   ├── main.rs              # 主入口
│   ├── lib.rs               # 库入口
│   ├── config.rs            # 配置管理
│   ├── context.rs           # 会话上下文
│   ├── agent/               # Agent 模块
│   │   ├── types.rs         # 类型定义
│   │   ├── builder.rs       # 构建器
│   │   └── subagent.rs      # 子代理管理
│   ├── cli/                 # CLI 模块
│   │   ├── mod.rs           # CLI 实现
│   │   ├── command.rs       # 命令处理
│   │   └── render.rs        # 渲染模块
│   ├── skill/               # 技能系统
│   │   ├── manager.rs       # 技能管理器
│   │   ├── executor.rs      # 技能执行器
│   │   └── loader.rs        # 技能加载器
│   ├── tools/               # 工具实现
│   │   ├── mod.rs
│   │   ├── read_file.rs
│   │   ├── write_file.rs
│   │   ├── edit_file.rs
│   │   └── ...
│   ├── task/                # 任务管理
├── openspec/                # OpenSpec 规范
│   ├── specs/               # 当前能力规范
│   ├── changes/             # 变更提案
│   └── AGENTS.md            # 代理指令
├── docs/                    # 文档
│   └── architecture.md      # 本文档
├── tests/                   # 测试文件
├── Cargo.toml               # Rust 项目配置
└── package.json             # Node.js 包配置
```

## 性能优化

### 异步处理

- 所有 I/O 操作使用 Tokio 异步运行时
- 并行工具执行
- 非阻塞用户交互

### 缓存策略

- **技能缓存**: `Lazy + RwLock` 避免重复加载
- **会话缓存**: 内存中保持活跃会话
- **配置缓存**: 启动时一次性加载配置

### 按需加载

- 工具按需初始化
- 技能延迟加载
- 减少启动时间

## 扩展性

### 添加新工具

实现 `Tool` trait：

```rust
pub struct MyTool;

impl Tool for MyTool {
    const NAME: &'static str = "my_tool";
    // ... 实现 trait 方法
}
```

### 添加新技能

在 `.oxide/skills/` 或 `~/.oxide/skills/` 创建 Markdown 文件：

```markdown
---
description: "我的技能"
triggers: ["my", "custom"]
---

技能内容...
```

### 添加新 Agent 类型

在 `agent/types.rs` 中添加新类型并实现构建逻辑。

## 安全性

### 权限控制

- 只读 Agent（Explore）限制写入操作
- Shell 执行需要用户确认
- 文件操作路径验证

### 敏感信息

- API 密钥通过环境变量配置
- 不在代码中硬编码密钥
- 支持密钥轮换

### 输入验证

- 文件路径白名单
- Shell 命令参数转义
- LLM 输入 sanitization

## 开发工作流

### OpenSpec 规范驱动

项目采用 OpenSpec 进行规范驱动的开发：

1. **创建变更提案**: `openspec/changes/{change-name}/proposal.md`
2. **实现规范**: 在 `specs/` 目录定义能力规范
3. **任务跟踪**: 在 `tasks.md` 中跟踪实现进度
4. **归档变更**: 完成后移至 `archive/`

### 测试策略

- 单元测试：核心逻辑模块
- 集成测试：工具和 Agent 集成
- 端到端测试：CLI 工作流

### 发布流程

1. 更新 `Cargo.toml` 和 `package.json` 版本
2. 运行测试套件
3. 构建 Rust 二进制文件
4. 打包 npm 包
5. 发布到 crates.io 和 npm

## 参考资源

- [Rust 文档](https://doc.rust-lang.org/)
- [Tokio 文档](https://tokio.rs/)
- [Rig 框架](https://github.com/danieloi/rig)
- [OpenAI Function Calling](https://platform.openai.com/docs/guides/function-calling)
- [项目 README](../README.md)
- [使用指南](../USAGE.md)
