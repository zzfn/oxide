// Oxide CLI - npm entry point
// This is a placeholder; the actual binary is invoked via the `bin` field in package.json

module.exports = {
  name: 'oxide-cli',
  version: require('./package.json').version,
  description: 'AI-powered command line assistant with multi-LLM support'
};
