# 任务管理系统 - 快速参考

## 🚀 快速开始

### 启动 CLI
```bash
cargo run --release
```

### 基本测试流程

1. **创建任务**
   ```
   你: 请使用 TaskCreate 创建一个任务，标题是"实现用户认证"，描述是"实现 JWT 认证系统"
   ```

2. **列出任务**
   ```
   你: 使用 TaskList 列出所有任务
   ```

3. **查看详情**
   ```
   你: 使用 TaskGet 查看任务 #1 的详情
   ```

4. **更新状态**
   ```
   你: 使用 TaskUpdate 将任务 #1 的状态改为 in_progress
   ```

5. **创建依赖**
   ```
   你: 创建任务"编写测试"，然后用 TaskUpdate 让它依赖任务 #1
   ```

## 📋 工具参数速查

### TaskCreate
```json
{
  "subject": "任务标题",
  "description": "详细描述",
  "activeForm": "进行中文本（可选）",
  "metadata": {}  // 可选
}
```

### TaskList
```json
{}  // 无参数
```

### TaskGet
```json
{
  "taskId": "1"
}
```

### TaskUpdate
```json
{
  "taskId": "1",
  "status": "in_progress",      // 可选
  "addBlockedBy": ["0"],        // 可选：依赖关系
  "metadata": {"key": "value"}  // 可选
}
```

## 🎯 测试场景

### 场景 1: 简单任务管理
```
1. 创建任务 "实现登录功能"
2. 列出所有任务
3. 将任务状态改为 in_progress
4. 完成后改为 completed
```

### 场景 2: 任务依赖
```
1. 创建任务 A "设计数据库"
2. 创建任务 B "实现 API"，依赖 A
3. 创建任务 C "编写测试"，依赖 B
4. 列出任务，查看依赖关系
```

### 场景 3: 元数据管理
```
1. 创建任务，添加 metadata: {"priority": "high", "assignee": "Alice"}
2. 查看任务详情，确认元数据
3. 更新元数据，添加新字段
```

## 🧪 单元测试

```bash
# 运行所有任务系统测试
cargo test -p oxide-tools task

# 运行特定测试
cargo test -p oxide-tools test_task_create_tool
cargo test -p oxide-tools test_circular_dependency_detection
```

## 📚 文档

- **完整文档**: `docs/task-system.md`
- **实现代码**: `crates/oxide-tools/src/task/`
- **测试代码**: `crates/oxide-tools/src/task/*/tests`

## 💡 提示

1. **任务 ID 是自增的数字**（1, 2, 3...）
2. **状态转换有规则**：pending → in_progress → completed → deleted
3. **循环依赖会被自动检测**并拒绝
4. **元数据可以存储任意 JSON 值**
5. **deleted 状态的任务不会在 TaskList 中显示**

## 🐛 故障排除

### 问题：AI 没有调用工具
- 确保你的提示明确要求使用工具
- 尝试："请使用 TaskCreate 工具..."

### 问题：循环依赖错误
- 检查依赖关系是否形成环
- 使用 TaskList 查看当前依赖图

### 问题：任务不存在
- 使用 TaskList 确认任务 ID
- 任务 ID 从 1 开始

## ✅ 验证清单

- [ ] 能创建任务
- [ ] 能列出任务
- [ ] 能查看任务详情
- [ ] 能更新任务状态
- [ ] 能设置任务依赖
- [ ] 循环依赖被正确拒绝
- [ ] 元数据正常工作

---

**版本**: 1.0
**更新**: 2026-01-30
